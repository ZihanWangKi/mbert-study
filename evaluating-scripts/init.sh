# install allennlp
pip install allennlp

# Add the current directory to python's path, e.g.
# echo /path/to/current/directory >> virtual_env/lib/python/site-packages/mpath.pth
# so that the custom allennlp script can be used